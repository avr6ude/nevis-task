declare const styles:
  & Readonly<{ "dashboard": string }>
  & Readonly<{ "panel": string }>
  & Readonly<{ "panelTable": string }>
  & Readonly<{ "chartPlaceholder": string }>
  & Readonly<{ "scope": string }>
  & Readonly<{ "scopeSep": string }>
;
export default styles;
